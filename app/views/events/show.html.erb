<h1>Events#show</h1>
<span>description: <%= @event.description%></span>
<span>creator: <%= @event.creator["name"]%></span>
<h3>Attendes:</h3>
<% @event.attendes.each do |attende| %>
  <span>attende: <%= attende["name"]%></span>
<% end %>

<%= link_to 'Back to events', events_path %>